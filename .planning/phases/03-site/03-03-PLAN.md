---
phase: 03-site
plan: "03"
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/pages/index.astro
autonomous: true
requirements: [SITE-04]
gap_closure: true

must_haves:
  truths:
    - "dist/index.html og:image URL is https://maidok.github.io/ee-scammers/og-preview.png (base path included)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Correct ogImage construction with /ee-scammers base path"
      contains: "ee-scammers/og-preview.png"
    - path: "dist/index.html"
      provides: "Built output with correct og:image meta tag"
      contains: "https://maidok.github.io/ee-scammers/og-preview.png"
  key_links:
    - from: "src/pages/index.astro"
      to: "dist/index.html"
      via: "npm run build"
      pattern: "maidok\\.github\\.io/ee-scammers/og-preview\\.png"
---

<objective>
Fix the og:image URL in src/pages/index.astro so the built dist/index.html emits the correct absolute URL https://maidok.github.io/ee-scammers/og-preview.png.

Purpose: Social media crawlers (Facebook, Telegram, Messenger) will fetch the og:image URL. The current URL omits the /ee-scammers base path, causing a 404 when the site is deployed as a GitHub Pages project page. Shared links will show no preview image.

Output: One-line fix in src/pages/index.astro + rebuilt dist/index.html with correct og:image URL.
</objective>

<execution_context>
@/Users/maidok/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maidok/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/index.astro

<interfaces>
<!-- Key context for the executor. -->

Gap from VERIFICATION.md:
- Truth 9 FAILED: og:image URL is `https://maidok.github.io/og-preview.png`
- Correct URL:     `https://maidok.github.io/ee-scammers/og-preview.png`
- Root cause: line 6 of src/pages/index.astro uses `new URL('og-preview.png', Astro.site)`
  where Astro.site = 'https://maidok.github.io' (no base path)

Fix options (any is acceptable):
  Option A: `new URL('/ee-scammers/og-preview.png', Astro.site).toString()`
  Option B: `new URL('og-preview.png', new URL('/ee-scammers/', Astro.site)).toString()`
  Option C: `` `${Astro.site}ee-scammers/og-preview.png` ``

Option A is the clearest. Use Option A.

From astro.config.mjs:
  site: 'https://maidok.github.io'
  base: '/ee-scammers'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ogImage URL to include /ee-scammers base path</name>
  <files>src/pages/index.astro</files>
  <action>
    On line 6 of src/pages/index.astro, replace:

      const ogImage = new URL('og-preview.png', Astro.site).toString();

    with:

      const ogImage = new URL('/ee-scammers/og-preview.png', Astro.site).toString();

    This produces https://maidok.github.io/ee-scammers/og-preview.png, which is the correct
    absolute URL for the og:image meta tag given the GitHub Pages project-page deployment.

    Do NOT use Astro.url — it resolves to the page URL, not the asset URL.
    Do NOT use the base config variable — hard-coding /ee-scammers here is intentional and
    consistent with the existing decision recorded in STATE.md.

    After editing index.astro, run:

      npm run build

    This rebuilds dist/index.html with the corrected og:image tag.
    Note: npm run build also runs scripts/og-screenshot.ts (Playwright), so Chromium must be
    available. If the build environment lacks Chromium, run `npx playwright install chromium`
    first, then retry npm run build.
  </action>
  <verify>
    <automated>grep 'og-preview' dist/index.html | grep 'ee-scammers'</automated>
  </verify>
  <done>
    dist/index.html contains an og:image meta tag whose content attribute is exactly
    https://maidok.github.io/ee-scammers/og-preview.png — verified by grep returning a match.
  </done>
</task>

</tasks>

<verification>
After the task completes, confirm:

1. `grep 'og:image' dist/index.html` output contains `/ee-scammers/og-preview.png`
2. `grep 'og:image' dist/index.html` output does NOT show `maidok.github.io/og-preview.png` (the broken URL without base path)
3. `npx vitest run` still passes all 7 tests (regression check)
</verification>

<success_criteria>
- dist/index.html og:image meta tag value is https://maidok.github.io/ee-scammers/og-preview.png
- All 7 vitest tests continue to pass
- SITE-04 gap from VERIFICATION.md is closed: OG meta tag references correct absolute URL
</success_criteria>

<output>
After completion, create `.planning/phases/03-site/03-03-SUMMARY.md` using the summary template.
</output>
